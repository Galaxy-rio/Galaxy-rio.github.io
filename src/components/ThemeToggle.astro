---
import MaterialSymbol from "./MaterialSymbol.astro";

interface Props {
  label: string;
  class?: string;
  text?: string;
}

const { label, class: className = "theme-toggle", text } = Astro.props;
---

<button
  class={className}
  type="button"
  data-theme-toggle
  aria-label={label}
  aria-pressed="false"
  title={label}
>
  <span data-theme-icon aria-hidden="true">
    <MaterialSymbol name="dark_mode" />
  </span>
  {text && <span class="theme-toggle-label">{text}</span>}
</button>
